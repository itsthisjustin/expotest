import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: { 
    position: 'relative' 
  },
  svgContainer: {
    position: 'absolute'
  }
});

export default styles;
